<!-- TRIP INFO -->
<div id="trip-info">
  <h3><%= @trip.trip_title %></h3>
  <!-- TODO: insert location here -->
  <% if @trip.start_date && @trip.end_date %>
    <p><%= @trip.start_date.strftime("%B %d, %Y") %> - <%= @trip.end_date.strftime("%B %d, %Y") %></p>
  <% else %>
    <% nil %>
  <% end %>

  <!-- TRIP OPTIONS: EDIT / DELETE -->
  <div class="dropdown">
    <button class="btn btn-secondary btn-sm dropdown-toggle" type="button" id="tripDropdownMenu" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Options</button>
    <div class="dropdown-menu" aria-labelledby="tripDropdownMenu">
      <button class="dropdown-item" type="button" data-toggle="modal" data-target="#editTripModal">Edit Trip</button>
      <div class="dropdown-divider"></div>
      <%= button_to "Delete", "/trips/#{@trip.id}", method: :delete, class: "dropdown-item", data: { confirm: 'Are you sure?' } %>
    </div>
  </div>
</div>

<!-- TRIP OPTIONS: TO ADD STUFF -->
<div id="trip-add-options">
  <!-- ADD BOARD MODAL -->
  <!-- MODAL TRIGGER -->
  <button class="btn btn-md btn-theme" 
    data-toggle="modal"
    data-target="#addBoardModal">Add Board</button>

  <!-- ADD LODGING MODAL -->
  <!-- MODAL TRIGGER -->
  <button class="btn btn-md btn-theme" 
    data-toggle="modal"
    data-target="#addLodgingModal">Add Lodging</button>

  <!-- ADD TRANSPORTATION MODAL -->
  <!-- MODAL TRIGGER -->
  <button class="btn btn-md btn-theme" 
    data-toggle="modal"
    data-target="#addTransportationModal">Add Transportation</button>
</div>


<!-- EDIT TRIP MODAL CONTENT --> 

  <div class="modal fade modal-center" id="editTripModal" tabindex="-1" role="dialog">
        <div class="modal-dialog modal-md" role="document">
            <div class="modal-content">
                <div class="modal-body no-pdd">
                    <div class="padding-30">
                        <h3 class="mrg-vertical-15">Edit <%= @trip.trip_title %> Trip</h3>
                        <%= form_tag "/trips/#{@trip.id}", method: :patch do  %>
                          <div class="modal-body">
                            <div class="form-group">
                              <label for="trip_title">Trip Title</label>
                              <input type="text" class="form-control" id="trip_title" name="trip_title" placeholder="New York, Destination Wedding, Etc." value="<%= @trip.trip_title %>">
                            </div>
                            <div class="form-group">
                              <label for="start_date">From</label>
                              <input type="date" class="form-control" id="start_date" name="start_date" value="<%= @trip.start_date %>">
                            </div>
                            <div class="form-group">
                              <label for="end_date">To</label>
                              <input type="date" class="form-control" id="end_date" name="end_date" value="<%= @trip.end_date %>">
                            </div>
                          </div>
                          <div class="text-right mrg-top-20">
                            <button type="button" class="btn btn-md btn-default border-radius-4" data-dismiss="modal">Cancel</button>
                            <button type="submit" class="btn btn-md btn-theme border-radius-4">Save Changes</button>
                          </div>
                        <% end %>
                    </div>
                    <div class="modal-close">
                        <button type="button" class="btn icon-btn-md icon-btn-round btn-dark lh-0" data-dismiss="modal"><i class="ei ei-close"></i></button>
                    </div>
                </div>
            </div>
        </div>
    </div>
  
<!-- ADD BOARD MODAL CONTENT -->
  <div class="modal fade" id="addBoardModal" tabindex="-1" role="dialog" aria-labelledby="addBoardModal" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h4 class="modal-title" id="myModalLabel">Add Board</h4>
          <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        </div>
        <%= form_tag "/trips/#{@trip.id}/boards", method: :post do  %>
          <div class="modal-body">
            <%= render partial: "board_form" %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-sm btn-theme">Add Board</button>
          </div>
        <% end %>
      </div>
    </div>
  </div>

<!-- ADD LODGING MODAL CONTENT -->
  <div class="modal fade" id="addLodgingModal" tabindex="-1" role="dialog" aria-labelledby="addLodgingModal" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h4 class="modal-title" id="myModalLabel">Add Lodging</h4>
          <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        </div>
        <%= form_tag "/trips/#{@trip.id}/lodgings", method: :post do  %>
          <div class="modal-body">
            <%= render partial: "lodging_form" %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-sm btn-theme">Add Lodging</button>
          </div>
        <% end %>
      </div>
    </div>
  </div>

<!-- ADD TRANSPORTATION MODAL CONTENT -->
  <div class="modal fade" id="addTransportationModal" tabindex="-1" role="dialog" aria-labelledby="addTransportationModal" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h4 class="modal-title" id="myModalLabel">Add Transportation</h4>
          <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        </div>
        <%= form_tag "/trips/#{@trip.id}/transportations", method: :post do  %>
          <div class="modal-body">
            <%= render partial: "transportation_form" %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-sm btn-theme">Add Transportation</button>
          </div>
        <% end %>
      </div>
    </div>
  </div>

<!-- LODGING LIST - EACH -->
<div id="lodging-list">
  <% @trip.lodgings.each do |lodging| %>
    
    <div id="lodging-each" class="btn btn-link" data-toggle="modal" data-target="#showLodging<%=lodging.id%>">
      <% if lodging.checkin_date && lodging.checkout_date %>
        <p><%= lodging.lodging_name %> | <%= lodging.checkin_date.strftime("%m/%d/%Y") %> - <%= lodging.checkout_date.strftime("%m/%d/%Y") %></p>
      <% else %>
        <p><%= lodging.lodging_name %></p>
      <% end %>
    </div>

<!-- SHOW EACH LODGING MODAL CONTENT -->
    <div class="modal fade" id="showLodging<%=lodging.id%>" tabindex="-1" role="dialog" aria-labelledby="showLodging<%=lodging.id%>" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h4 class="modal-title" id="myModalLabel"><%= lodging.lodging_name %></h4>
            <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          </div>
          <div class="modal-body">
            <div>
              <p>Check In: <%= lodging.checkin_date.strftime("%m/%d/%Y") if lodging.checkin_date %></p>
              <p>Check Out: <%= lodging.checkout_date.strftime("%m/%d/%Y") if lodging.checkout_date %></p>
              <hr>
              <p>Notes:</p>
              <p><%= lodging.notes %></p>
              <hr>
              <p>Price / Budget: <%= lodging.price %></p>
            </div>
          </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
              <button type="button" class="btn btn-sm btn-theme" data-toggle="modal" data-target="#editLodging<%=lodging.id%>">Edit</button>
              <%= button_to "Delete", "/trips/#{@trip.id}/lodgings/#{lodging.id}", method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' } %>
            </div>
        </div>
      </div>
    </div>

<!-- EDIT LODGING MODAL CONTENT -->

  <div class="modal fade" id="editLodging<%=lodging.id%>" tabindex="-1" role="dialog" aria-labelledby="editLodging<%=lodging.id%>" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h4 class="modal-title" id="myModalLabel">Edit Lodging</h4>
          <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        </div>
        <%= form_tag "/trips/#{@trip.id}/lodgings/#{lodging.id}", method: :patch do  %>
          <div class="modal-body">
            <div>
              <label for="lodging_name">Lodging Name</label>
              <input type="text" class="form-control" id="lodging_name" name="lodging_name" placeholder="Airbnb, Hilton Hotel, etc." value="<%= lodging.lodging_name %>">
            </div>
            <div>
              <label for="checkin_date">From</label>
              <input type="date" class="form-control" id="checkin_date" name="checkin_date" value="<%= lodging.checkin_date %>">
            </div>
            <div>
              <label for="checkout_date">To</label>
              <input type="date" class="form-control" id="checkout_date" name="checkout_date" value="<%= lodging.checkout_date %>">
            </div>
            <div>
              <label for="notes">Notes</label>
              <textarea class="form-control" id="notes" name="notes" placeholder="Confirmation No., Reminders, etc." maxlength="500"><%= lodging.notes %></textarea>
            </div>
            <div>
              <label for="price">Price / Budget</label>
              <input ype="number" min="0.00" max="999999.99" step="any" class="form-control" id="price" name="price" value="<%= lodging.price %>">
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-sm btn-theme">Save Changes</button>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <% end %>
</div>



<!-- TRANSPORTATION LIST - EACH -->
<div id="transpo-list">
  <% @trip.transportations.each do |transportation| %>
    <div id="transpo-each" class="btn btn-link" data-toggle="modal" data-target="#showTranspo<%=transportation.id%>">
      <% if transportation.depart_date %>
        <p><%= transportation.transpo_name %> | <%= transportation.depart_date.strftime("%m/%d/%Y")%></p>
      <% else %>
        <p><%= transportation.transpo_name %></p>
      <% end %>
    </div>

<!-- SHOW EACH TRANSPORTATION MODAL CONTENT -->
    <div class="modal fade" id="showTranspo<%=transportation.id%>" tabindex="-1" role="dialog" aria-labelledby="showTranspo<%=transportation.id%>" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h4 class="modal-title" id="myModalLabel"><%= transportation.transpo_type %></h4>
            <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          </div>
          <div class="modal-body">
            <div>
              <h5><%= transportation.transpo_name %></h5>
              <p>Departure: <%= transportation.depart_date.strftime("%m/%d/%Y") if transportation.depart_date %></p>
              <hr>
              <p>Notes:</p>
              <p><%= transportation.notes %></p>
              <hr>
              <p>Price / Budget: <%= transportation.price %></p>
            </div>
          </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
              <button type="button" class="btn btn-sm btn-theme" data-toggle="modal" data-target="#editTranspo<%=transportation.id%>">Edit</button>
              <%= button_to "Delete", "/trips/#{@trip.id}/transportations/#{transportation.id}", method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' } %>
            </div>
        </div>
      </div>
    </div>

<!-- EDIT TRANSPORTATION MODAL CONTENT -->

  <div class="modal fade" id="editTranspo<%=transportation.id%>" tabindex="-1" role="dialog" aria-labelledby="editTranspo<%=transportation.id%>" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h4 class="modal-title" id="myModalLabel">Edit Transportation</h4>
          <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        </div>
        <%= form_tag "/trips/#{@trip.id}/transportations/#{transportation.id}", method: :patch do  %>
          <div class="modal-body">
            <div>
              <select id="transpo_type" class="custom-select" name="transpo_type" value="<%= transportation.transpo_type %>">
                <option selected>Transportation Type</option>
                <option value="Flight">Flight</option>
                <option value="Car Rental">Car Rental</option>
                <option value="Rail">Rail</option>
                <option value="Ground Transportation">Ground Transportation</option>
                <option value="Ferry">Ferry</option>
                <option value="Cruise">Cruise</option>
                <option value="Other">Other</option>
              </select>
            </div>
            <div>
              <label for="transpo_name">Transportation Name</label>
              <input type="text" class="form-control" id="transpo_name" name="transpo_name" placeholder="Airline, Busline, etc." value="<%= transportation.transpo_name %>">
            </div>
            <div>
              <label for="depart_date">Departure Date</label>
              <input type="date" class="form-control" id="depart_date" name="depart_date" value="<%= transportation.depart_date %>">
            </div>
            <div>
              <label for="notes">Notes</label>
              <textarea class="form-control" id="notes" name="notes" placeholder="Confirmation No., Reminders, etc." maxlength="500"><%= transportation.notes %></textarea>
            </div>
            <div>
              <label for="price">Price / Budget</label>
              <input ype="number" min="0.00" max="999999.99" step="any" class="form-control" id="price" name="price" value="<%= transportation.price %>">
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-sm btn-theme">Save Changes</button>
          </div>
        <% end %>
      </div>
    </div>
  </div>


  <% end %>
</div>


<!-- BOARDS LIST - EACH -->
<% @trip.boards.each do |board| %>
  <div id="board-each">
<!-- BOARD HEADER -->
    <div class="board-header">
      <h5><%= board.board_title %></h5>
    </div>
<!-- BOARD OPTIONS -->
    <div class="dropdown">
      <button class="btn btn-secondary btn-sm dropdown-toggle" type="button" id="dropdownMenu2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Options
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenu2">
        <button class="dropdown-item" type="button" data-toggle="modal" data-target="#editBoard<%=board.id%>">Edit Board</button>
        <button class="dropdown-item" type="button" data-toggle="modal" data-target="#addItem<%=board.id%>">Add Item</button>
        <div class="dropdown-divider"></div>
        <!-- <button class="dropdown-item" type="button">Archive</button> TODO: Archive Board -->
        <%= button_to "Delete", "/trips/#{@trip.id}/boards/#{board.id}", method: :delete, class: "dropdown-item", data: { confirm: 'Are you sure?' } %> 
      </div>
    </div>

<!-- EDIT BOARD MODAL CONTENT -->
      <div class="modal fade" id="editBoard<%=board.id%>" tabindex="-1" role="dialog" aria-labelledby="editBoard<%=board.id%>" aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h4 class="modal-title" id="myModalLabel">Edit <%= board.board_title %></h4>
              <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            </div>
            <%= form_tag "/trips/#{@trip.id}/boards/#{board.id}", method: :patch do  %>
              <div class="modal-body">
                <div>
                  <label for="board_title">Board Title</label>
                  <input type="text" class="form-control" id="board_title" name="board_title" placeholder="First Day in NYC, Food List, Etc." value="<%= board.board_title %>">
                </div>

              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-sm btn-theme">Save Changes</button>
              </div>
            <% end %>
          </div>
        </div>
      </div>

<!-- ADD ITEM MODAL CONTENT -->
      <div class="modal fade" id="addItem<%=board.id%>" tabindex="-1" role="dialog" aria-labelledby="addItem<%=board.id%>" aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h4 class="modal-title" id="myModalLabel">Add Item <%= board.board_title %></h4>
              <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            </div>
            <%= form_tag "/trips/#{@trip.id}/boards/#{board.id}/items", method: :post do  %>
              <div class="modal-body">
                <div>
                  <%= render partial: "item_form" %>
                </div>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-sm btn-theme">Add Item</button>
              </div>
            <% end %>
          </div>
        </div>
      </div>

<!-- ITEMS LIST -->
      <% board.items.each do |item| %>
        <div id="item-each" class="btn btn-link" data-toggle="modal" data-target="#showItem<%=item.id%>">
          <%= item.description %> | <%= item.item_type %>
        </div><br> <!-- TODO: show only a few characters of the notes--> 

<!-- SHOW ITEM MODAL CONTENT -->
        <div class="modal fade" id="showItem<%=item.id%>" tabindex="-1" role="dialog" aria-labelledby="showItem<%=item.id%>" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel"><%= item.item_type %></h4>
                <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
              </div>
              <div class="modal-body">
                <div>
                  <p><%= item.description %></p>
                  <hr>
                  <p>Price / Budget: <%= item.price %></p>
                </div>
              </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                  <button type="button" class="btn btn-sm btn-theme" data-toggle="modal" data-target="#editItem<%=item.id%>">Edit</button>
                  <!-- MOVE BUTTON -->
                  <div class="btn-group" role="group">
                    <button id="btnGroupDrop1" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Move
                    </button>
                    <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                      <% @trip.boards.each do |board|%>
                        <% if item.board_id != board.id %>
                          <%= form_tag "/trips/#{@trip.id}/boards/#{board.id}/items/#{item.id}", method: :patch do  %>
                            <button class="dropdown-item" name="board_id" value="<%= board.id %>"><%= board.board_title %></button>
                          <% end %>
                        <% end %>
                      <% end %>
                    </div>
                  </div>
                  <!-- END OF MOVE -->
                  <%= button_to "Delete", "/trips/#{@trip.id}/boards/#{board.id}/items/#{item.id}", method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' } %>
                </div>
            </div>
          </div>
        </div>


<!-- EDIT ITEM MODAL CONTENT -->

        <div class="modal fade" id="editItem<%=item.id%>" tabindex="-1" role="dialog" aria-labelledby="editItem<%=item.id%>" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel">Edit Item</h4>
                <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
              </div>
              <%= form_tag "/trips/#{@trip.id}/boards/#{board.id}/items/#{item.id}", method: :patch do  %>
                <div class="modal-body">
                  <div>
                    <div class="form-check form-check-inline" value="<%= item.item_type %>">
                      <label class="form-check-label">
                        <input class="form-check-input" type="radio" name="item_type" id="inlineRadio1" value="Food / Drink"> Food / Drink
                      </label> <!-- TODO: conditional checked="checked" -->
                    </div>
                    <div class="form-check form-check-inline" value="<%= item.item_type %>">
                      <label class="form-check-label">
                        <input class="form-check-input" type="radio" name="item_type" id="inlineRadio2" value="Activity"> Activity
                      </label>
                    </div>

                    <div>
                      Description:<br>
                      <textarea class="form-control" name="description" maxlength="500"><%= item.description %></textarea>
                    </div>
                    
                    <div>
                      <label for="price">Price / Budget</label>
                      <input type="number" min="0.00" max="999999.99" step="any" class="form-control" id="price" name="price" placeholder="$" value="<%= item.price %>">
                    </div>

                  </div>
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                  <button type="submit" class="btn btn-sm btn-theme">Save Changes</button>
                </div>
              <% end %>
            </div>
          </div>
        </div>


      <% end %> <!-- END OF ITEM-->
  
  </div>  
  <br><br>
<% end %> <!-- END OF BOARD -->


